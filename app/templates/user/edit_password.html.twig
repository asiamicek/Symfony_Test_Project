{% extends 'base.html.twig' %}

{% block title %}
    {{ 'title_user_edit'|trans({'%id%': user.id|default('')}) }}
{% endblock %}

{% block body %}
    {% if is_granted('ROLE_ADMIN') or app.user %}
{% include 'default/_form.html.twig' with {
    page_title: 'title.user_passsword_edit'|trans({'%id%': user.id|default('')}),
    form: form,
    submit_label: 'action.edit'|trans,
    back_to_list_path: 'app_login',
} %}
    {% endif %}
{% endblock %}